{
  "name": "@oblivion/contracts",
  "version": "1.0.0",
  "description": "Midnight smart contracts for Oblivion Protocol",
  "main": "dist/index.js",
  "scripts": {
    "build": "compact compile src/DataCommitment.compact && compact compile src/ZKDeletionVerifier.compact",
    "deploy": "ts-node scripts/deploy.ts",
    "test": "jest"
  },
  "dependencies": {
    "@midnight-ntwrk/compact-runtime": "^0.7.0",
    "@midnight-ntwrk/midnight-js-types": "^0.4.0",
    "@midnight-ntwrk/midnight-js-network-api": "^0.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "ts-node": "^10.9.0",
    "jest": "^29.0.0",
    "@types/jest": "^29.0.0"
  }
}
